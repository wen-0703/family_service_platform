server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/family_service_platform?useSSL=false&useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: qiyawen123

mybatis:
  mapper-locations: classpath:com/wen/mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com:
      wen:
        mapper: debug